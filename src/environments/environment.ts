// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firebase: {
    apiKey: 'AIzaSyDrrGrUBrmW2n2W-i_-RE3O1oLvCGORjDM',
    authDomain: 'quiz-fighter-2918d.firebaseapp.com',
    databaseURL: 'https://quiz-fighter-2918d.firebaseio.com',
    projectId: 'quiz-fighter-2918d',
    storageBucket: 'quiz-fighter-2918d.appspot.com',
    messagingSenderId: '958327954822'
  }
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
